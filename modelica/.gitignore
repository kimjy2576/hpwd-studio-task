# OpenModelica(omc) 빌드 산출물 전부 무시, 소스만 추적
# (.o .c .h .json .xml .mat .makefile .log .libs .bin, 모델명 실행파일 등)
*
!.gitignore
!*/
!*.mo
!*.py
!*.md
!*.mos

# 개인 HelmholtzMedia 경로 사이드카 (각자 로컬, 추적 안 함)
helmholtz_path.mos

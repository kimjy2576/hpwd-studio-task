"""cancel — 계산 중단 신호 (2026-07-31).

파이썬 스레드는 강제 종료할 수 없어 협조적 중단만 가능하다.
진행 콜백에서 이 예외를 올리면 솔버가 빠져나온다.

솔버의 콜백 호출부는 '보고 실패가 계산을 막지 않도록' except Exception 으로
감싸는데, 그것이 취소까지 삼키면 안 된다. 그래서 전용 예외를 두고
가드가 이것만 통과시킨다.
"""


class Cancelled(Exception):
    """사용자 중단 요청."""
